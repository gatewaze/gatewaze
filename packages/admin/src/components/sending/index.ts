export { SendingPanel } from './SendingPanel';
export type {
  SendingAdapter, SendRecord, EmailDetails, EmailDetailsControl, RecipientsControl,
  ScheduleType, DeliveryStrategy, SendComposerConfig,
} from './types';
